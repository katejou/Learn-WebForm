$(document).ready(function () {

    // spellcheck , autocomplete

    $('form').attr('autocomplete', 'off');
    $("form").attr("spellcheck", "false");

});  // 這個是 jQuery(document).ready(function ($) 的終點